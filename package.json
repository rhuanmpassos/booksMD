{
  "name": "booksmd",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build -- --configuration production",
    "vercel-build": "cd frontend && npm install && npm run build -- --configuration production"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  }
}
