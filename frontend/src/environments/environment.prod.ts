export const environment = {
  production: true,
  // ATENÇÃO: Se o backend estiver em outro domínio, atualize esta URL
  // Exemplo: apiUrl: 'https://seu-backend.railway.app'
  // Ou configure via script de build (ver DEPLOY_VERCEL.md)
  apiUrl: '' // Same origin in production (backend no mesmo domínio)
};

